import {LazyEmailDirective} from './lazy-email.directive';

describe('LazyEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyEmailDirective();
    expect(directive).toBeTruthy();
  });
});
