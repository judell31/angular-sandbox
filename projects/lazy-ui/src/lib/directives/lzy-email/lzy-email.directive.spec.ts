import { LzyEmailDirective } from './lzy-email.directive';

describe('LzyEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new LzyEmailDirective();
    expect(directive).toBeTruthy();
  });
});
